# Sovereign Vision - pinned runtime dependencies.
# Tested on Apple Silicon (M-series) with Python 3.10 through 3.13.

# Computer vision + numerical
numpy>=2.0
opencv-python>=4.10
Pillow>=11.0

# MLX (Apple Silicon native ML runtime)
# Comment out if installing on Intel/Linux; the simulation backend is used instead.
mlx>=0.20 ; sys_platform == "darwin" and platform_machine == "arm64"

# Premium terminal UI for the secondary live dashboard
rich>=13.9

# Data modelling
pydantic>=2.10

# Logging / structured output
structlog>=24.4

# YAML scenario configs
PyYAML>=6.0

# Testing
pytest>=8.3
pytest-cov>=6.0
hypothesis>=6.100

# Architecture diagram rendering (optional but referenced from README)
matplotlib>=3.9

# Dev tooling (optional but pinned for reproducibility)
ruff>=0.8
mypy>=1.13
